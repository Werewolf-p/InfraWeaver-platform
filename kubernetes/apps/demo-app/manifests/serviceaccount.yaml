---
# Dedicated ServiceAccount — apps should not use the default SA.
# automountServiceAccountToken: false prevents token projection unless explicitly needed.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: demo-app
  namespace: apps-demo-app
automountServiceAccountToken: false
