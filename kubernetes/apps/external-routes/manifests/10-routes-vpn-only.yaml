---
# IngressRoutes for *.int.rlservers.com — private/VPN-only services.
#
# Security model:
#   - Cloudflare DNS-only wildcard: *.int.rlservers.com → 10.10.0.200 (no proxy)
#   - 10.10.0.200 is a private VLAN3 IP — unreachable from the public internet
#   - No IP allowlist middleware needed: the private IP IS the access control
#   - Anyone who can reach 10.10.0.200 is already on VLAN3 or VPN (trusted)
#   - This solves the browser DoH bypass problem with *.rlservers.com routes
#
# TLS: int-rlservers-com-tls (wildcard cert via letsencrypt-cloudflare DNS-01)

# ── NetBird Dashboard ─────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: netbird-dashboard-int
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`netbird.int.rlservers.com`)
      kind: Rule
      services:
        - name: netbird-dashboard
          port: 80
  tls:
    secretName: int-rlservers-com-tls
---
# ── ArgoCD ────────────────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-int
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`argocd.int.rlservers.com`)
      kind: Rule
      services:
        - name: argocd-server
          port: 80
  tls:
    secretName: int-rlservers-com-tls
---
# ── Grafana ───────────────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana-int
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`grafana.int.rlservers.com`)
      kind: Rule
      services:
        - name: grafana-apps
          port: 80
  tls:
    secretName: int-rlservers-com-tls
---
# ── Longhorn ──────────────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: longhorn-int
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`longhorn.int.rlservers.com`)
      kind: Rule
      services:
        - name: longhorn-frontend
          port: 80
  tls:
    secretName: int-rlservers-com-tls
---
# ── OpenBao (Vault) ───────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: openbao-int
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`openbao.int.rlservers.com`)
      kind: Rule
      services:
        - name: openbao
          port: 8200
  tls:
    secretName: int-rlservers-com-tls
