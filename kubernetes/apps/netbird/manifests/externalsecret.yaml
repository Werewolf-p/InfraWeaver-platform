---
# ── NetBird Secrets ExternalSecret ───────────────────────────────────────────
# Reads turn-password, datastore-enc-key, and SETUP_KEY from OpenBao.
# Bootstrap step generates these secrets on first deploy (stored at secret/platform/netbird).
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: netbird-secrets
  namespace: netbird
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: netbird-secrets
    creationPolicy: Owner
  data:
    - secretKey: turn-password
      remoteRef:
        key: secret/platform/netbird
        property: turn-password
    - secretKey: datastore-enc-key
      remoteRef:
        key: secret/platform/netbird
        property: datastore-enc-key
    - secretKey: SETUP_KEY
      remoteRef:
        key: secret/platform/netbird
        property: SETUP_KEY
