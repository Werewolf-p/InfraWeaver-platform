global:
  # Mount custom branding images as static files so they are served
  # permanently at /static/dist/assets/icons/<filename> (no JWT expiry).
  # subPath mounts add individual files without replacing the whole directory.
  volumes:
    - name: authentik-media
      configMap:
        name: authentik-media
  volumeMounts:
    - name: authentik-media
      mountPath: /web/dist/assets/icons/logo.png
      subPath: logo.png
      readOnly: true
    - name: authentik-media
      mountPath: /web/dist/assets/icons/favicon.png
      subPath: favicon.png
      readOnly: true
    - name: authentik-media
      mountPath: /web/dist/assets/icons/banner.jpg
      subPath: banner.jpg
      readOnly: true

  # Inject secrets from ExternalSecret-managed K8s secret (authentik-secrets)
  env:
    - name: AUTHENTIK_SECRET_KEY
      valueFrom:
        secretKeyRef:
          name: authentik-secrets
          key: secret-key
    - name: AUTHENTIK_BOOTSTRAP_PASSWORD
      valueFrom:
        secretKeyRef:
          name: authentik-secrets
          key: bootstrap-password
    - name: AUTHENTIK_BOOTSTRAP_TOKEN
      valueFrom:
        secretKeyRef:
          name: authentik-secrets
          key: bootstrap-token
    - name: AUTHENTIK_BOOTSTRAP_EMAIL
      value: "admin@rlservers.com"
    - name: AUTHENTIK_REMON_PASSWORD
      valueFrom:
        secretKeyRef:
          name: authentik-secrets
          key: remon-password
    - name: AUTHENTIK_ARDATY_PASSWORD
      valueFrom:
        secretKeyRef:
          name: authentik-secrets
          key: ardaty-password
    - name: AUTHENTIK_TESTUSER1_PASSWORD
      valueFrom:
        secretKeyRef:
          name: authentik-secrets
          key: testuser1-password
    - name: AUTHENTIK_POSTGRESQL__PASSWORD
      valueFrom:
        secretKeyRef:
          name: authentik-secrets
          key: postgresql-password
    - name: AUTHENTIK_EMAIL__USERNAME
      valueFrom:
        secretKeyRef:
          name: authentik-smtp-secret
          key: smtp-username
    - name: AUTHENTIK_EMAIL__PASSWORD
      valueFrom:
        secretKeyRef:
          name: authentik-smtp-secret
          key: smtp-password

authentik:
  # secret_key must be non-empty for chart templating; actual value from env var above
  secret_key: "placeholder-overridden-by-env"
  log_level: info
  error_reporting:
    enabled: false
  email:
    host: "smtp-mail.outlook.com"
    port: 587
    use_tls: true
    use_ssl: false
    timeout: 30
    from: "admin@rlservers.com"
  postgresql:
    host: "authentik-postgresql"
    name: "authentik"
    user: "authentik"
    password: ""  # supplied via AUTHENTIK_POSTGRESQL__PASSWORD env var
    port: 5432
  redis:
    host: "authentik-redis-master"

blueprints:
  configMaps:
    - authentik-blueprint-branding
    - authentik-blueprint-netbird
    - authentik-blueprint-users
    - authentik-blueprint-apps
    - authentik-blueprint-access-control

postgresql:
  enabled: true
  auth:
    username: authentik
    database: authentik
    existingSecret: authentik-secrets
    secretKeys:
      userPasswordKey: postgresql-password
  primary:
    persistence:
      storageClass: longhorn-retain   # HA: 3 replicas across nodes (critical user data)
      size: 2Gi
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: 500m
        memory: 512Mi

redis:
  enabled: true
  auth:
    enabled: false
  master:
    persistence:
      storageClass: longhorn   # HA: 3 replicas (rebuild-able cache)
      size: 256Mi
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        cpu: 200m
        memory: 128Mi

server:
  ingress:
    enabled: false
  # Give enough time for Django migrations on a fresh DB (292+ migrations)
  # NOTE: Authentik chart does NOT support livenessProbe.enabled field — use raw k8s probe fields only
  livenessProbe:
    initialDelaySeconds: 120
    periodSeconds: 10
    failureThreshold: 30
  readinessProbe:
    initialDelaySeconds: 120
    periodSeconds: 10
    failureThreshold: 30
  resources:
    requests:
      cpu: 200m
      memory: 512Mi
    limits:
      cpu: 1000m
      memory: 1Gi

worker:
  # Give enough time for Django migrations on a fresh DB (292+ migrations)
  # NOTE: Authentik chart does NOT support livenessProbe.enabled field — use raw k8s probe fields only
  livenessProbe:
    initialDelaySeconds: 120
    periodSeconds: 10
    failureThreshold: 30
  readinessProbe:
    initialDelaySeconds: 120
    periodSeconds: 10
    failureThreshold: 30
  resources:
    requests:
      cpu: 100m
      memory: 512Mi
    limits:
      cpu: 500m
      memory: 1Gi
