installCRDs: true

replicaCount: 1

webhook:
  replicaCount: 1

certController:
  replicaCount: 1

serviceMonitor:
  enabled: true

# Self-healing: ensure external-secrets operator is never evicted
priorityClassName: platform-critical
