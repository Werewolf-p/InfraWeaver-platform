apiVersion: v1
kind: LimitRange
metadata:
  name: longhorn-instance-manager-limits
  namespace: longhorn-system
spec:
  limits:
  - type: Container
    default:
      memory: 1500Mi
    defaultRequest:
      memory: 256Mi
