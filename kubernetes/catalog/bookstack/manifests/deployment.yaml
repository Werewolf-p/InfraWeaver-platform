---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/linuxserver/templates/main/unraid/bookstack.xml
# Category: Tools-Utilities, Productivity
# Stars: 348 | Downloads: 74121835
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookstack
  namespace: bookstack
  labels:
    app.kubernetes.io/name: bookstack
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: bookstack
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bookstack
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: bookstack
          image: lscr.io/linuxserver/bookstack
          env:
            - name: APP_URL
              value: ""
            - name: APP_KEY
              valueFrom:
                secretKeyRef:
                  name: app-key-secret
                  key: value
            - name: DB_HOST
              value: ""
            - name: DB_PORT
              value: "3306"
            - name: DB_USERNAME
              value: ""
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-password-secret
                  key: value
            - name: DB_DATABASE
              value: ""
            - name: QUEUE_CONNECTION
              value: ""
            - name: PUID
              value: "99"
            - name: PGID
              value: "100"
            - name: UMASK
              value: "022"
            - name: TZ
              value: "UTC"
          ports:
            - containerPort: 80
              protocol: TCP
              name: webui
          startupProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 120
          readinessProbe:
            tcpSocket:
              port: 80
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 80
            periodSeconds: 30
            failureThreshold: 3
          volumeMounts:
            - name: bookstack-data-0
              mountPath: "/config"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
      - name: bookstack-data-0
        persistentVolumeClaim:
          claimName: bookstack-data-0
---
apiVersion: v1
kind: Service
metadata:
  name: bookstack
  namespace: bookstack
  labels:
    app.kubernetes.io/name: bookstack
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: bookstack
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: webui
  type: ClusterIP