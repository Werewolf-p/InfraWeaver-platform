---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/linuxserver/templates/main/unraid/jackett.xml
# Category: Downloaders, Tools-Utilities
# Stars: 1205 | Downloads: 1570052728
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jackett
  namespace: jackett
  labels:
    app.kubernetes.io/name: jackett
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: jackett
  template:
    metadata:
      labels:
        app.kubernetes.io/name: jackett
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: jackett
          image: lscr.io/linuxserver/jackett
          env:
            - name: AUTO_UPDATE
              value: "true"
            - name: RUN_OPTS
              value: ""
            - name: PUID
              value: "99"
            - name: PGID
              value: "100"
            - name: UMASK
              value: "022"
            - name: TZ
              value: "UTC"
          ports:
            - containerPort: 9117
              protocol: TCP
              name: webui
          startupProbe:
            tcpSocket:
              port: 9117
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 120
          readinessProbe:
            tcpSocket:
              port: 9117
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 9117
            periodSeconds: 30
            failureThreshold: 3
          volumeMounts:
            - name: jackett-data-0
              mountPath: "/config"
            - name: jackett-data-1
              mountPath: "/downloads"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
      - name: jackett-data-0
        persistentVolumeClaim:
          claimName: jackett-data-0
      - name: jackett-data-1
        persistentVolumeClaim:
          claimName: jackett-data-1
---
apiVersion: v1
kind: Service
metadata:
  name: jackett
  namespace: jackett
  labels:
    app.kubernetes.io/name: jackett
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: jackett
  ports:
    - port: 9117
      targetPort: 9117
      protocol: TCP
      name: webui
  type: ClusterIP