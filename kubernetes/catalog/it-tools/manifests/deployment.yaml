---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/UnToobed/Unraid-Docker-Templates/main/Templates/IT-Tools.xml
# Category: Productivity, Security, Tools-Utilities
# Stars: 224 | Downloads: 18955660
apiVersion: apps/v1
kind: Deployment
metadata:
  name: it-tools
  namespace: iwt01-it-tools
  labels:
    app.kubernetes.io/name: it-tools
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: it-tools
  template:
    metadata:
      labels:
        app.kubernetes.io/name: it-tools
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: it-tools
          image: corentinth/it-tools
          # No environment variables defined
          ports:
            - containerPort: 80
              protocol: TCP
              name: host-port-1
          readinessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 30
            failureThreshold: 10
          livenessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 30
            failureThreshold: 10
          # No volume mounts defined
          securityContext:
            runAsNonRoot: false
            allowPrivilegeEscalation: false
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      # No volumes defined
---
apiVersion: v1
kind: Service
metadata:
  name: it-tools
  namespace: iwt01-it-tools
  labels:
    app.kubernetes.io/name: it-tools
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: it-tools
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: host-port-1
  type: ClusterIP