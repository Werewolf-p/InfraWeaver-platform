---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/aglovewithoutlove/unraid-templates/main/it-tools/it-tools.xml
# Category: Tools-Utilities
# Stars: 2 | Downloads: 457001
apiVersion: apps/v1
kind: Deployment
metadata:
  name: it-tools
  namespace: it-tools-test
  labels:
    app.kubernetes.io/name: it-tools
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: it-tools
  template:
    metadata:
      labels:
        app.kubernetes.io/name: it-tools
    spec:
      enableServiceLinks: false
      securityContext:
        fsGroup: 1000
      containers:
        - name: it-tools
          image: ghcr.io/sharevb/it-tools:latest
          # No environment variables defined
          ports:
            - containerPort: 8080
              protocol: TCP
              name: web-ui
          startupProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            tcpSocket:
              port: 8080
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 8080
            periodSeconds: 30
            failureThreshold: 3
          # No volume mounts defined
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      # No volumes defined
---
apiVersion: v1
kind: Service
metadata:
  name: it-tools
  namespace: it-tools-test
  labels:
    app.kubernetes.io/name: it-tools
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: it-tools
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: web-ui
  type: ClusterIP