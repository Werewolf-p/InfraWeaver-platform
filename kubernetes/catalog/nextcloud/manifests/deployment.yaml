---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/linuxserver/templates/main/unraid/nextcloud.xml
# Category: Cloud, Tools-Utilities
# Stars: 849 | Downloads: 323956204
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nextcloud
  namespace: nextcloud
  labels:
    app.kubernetes.io/name: nextcloud
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: nextcloud
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nextcloud
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: nextcloud
          image: lscr.io/linuxserver/nextcloud
          env:
            - name: PUID
              value: "99"
            - name: PGID
              value: "100"
            - name: UMASK
              value: "022"
            - name: TZ
              value: "UTC"
          ports:
            - containerPort: 443
              protocol: TCP
              name: webui
          startupProbe:
            tcpSocket:
              port: 443
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 120
          readinessProbe:
            tcpSocket:
              port: 443
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 443
            periodSeconds: 30
            failureThreshold: 3
          volumeMounts:
            - name: nextcloud-data-0
              mountPath: "/config"
            - name: nextcloud-data-1
              mountPath: "/data"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "2000m"
      volumes:
      - name: nextcloud-data-0
        persistentVolumeClaim:
          claimName: nextcloud-data-0
      - name: nextcloud-data-1
        persistentVolumeClaim:
          claimName: nextcloud-data-1
---
apiVersion: v1
kind: Service
metadata:
  name: nextcloud
  namespace: nextcloud
  labels:
    app.kubernetes.io/name: nextcloud
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: nextcloud
  ports:
    - port: 443
      targetPort: 443
      protocol: TCP
      name: webui
  type: ClusterIP