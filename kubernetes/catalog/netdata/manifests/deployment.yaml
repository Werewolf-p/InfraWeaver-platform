---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/Data-Monkey/docker-templates/master/Data-Monkey/netdata.xml
# Category: Tools-Utilities
# Stars: 569 | Downloads: 512699373
apiVersion: apps/v1
kind: Deployment
metadata:
  name: netdata
  namespace: netdata
  labels:
    app.kubernetes.io/name: netdata
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: netdata
  template:
    metadata:
      labels:
        app.kubernetes.io/name: netdata
    spec:
      hostNetwork: true
      securityContext:
        fsGroup: 1000
      containers:
        - name: netdata
          image: netdata/netdata
          env:
            - name: PGID
              value: "281"
            - name: DO_NOT_TRACK
              value: "1"
            - name: NETDATA_CLAIM_TOKEN
              value: ""
            - name: NETDATA_CLAIM_URL
              value: ""
            - name: NETDATA_CLAIM_ROOMS
              value: ""
            - name: NETDATA_EXTRA_DEB_PACKAGES
              value: ""
          # No ports defined

          volumeMounts:
            - name: netdata-data-0
              mountPath: "/host/etc/passwd"
            - name: netdata-data-1
              mountPath: "/host/etc/group"
            - name: netdata-data-2
              mountPath: "/etc/localtime"
            - name: netdata-data-3
              mountPath: "/host/proc"
            - name: netdata-data-4
              mountPath: "/host/sys"
            - name: netdata-data-5
              mountPath: "/host/etc/os-release"
            - name: netdata-data-6
              mountPath: "/host/var/log"
            - name: netdata-data-7
              mountPath: "/var/run/docker.sock"
            - name: netdata-data-8
              mountPath: "/run/dbus"
            - name: netdata-data-9
              mountPath: "/etc/netdata"
            - name: netdata-data-10
              mountPath: "/var/lib/netdata"
            - name: netdata-data-11
              mountPath: "/var/cache/netdata"
          securityContext:
            runAsNonRoot: false
            allowPrivilegeEscalation: false
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
      - name: netdata-data-0
        persistentVolumeClaim:
          claimName: netdata-data-0
      - name: netdata-data-1
        persistentVolumeClaim:
          claimName: netdata-data-1
      - name: netdata-data-2
        persistentVolumeClaim:
          claimName: netdata-data-2
      - name: netdata-data-3
        persistentVolumeClaim:
          claimName: netdata-data-3
      - name: netdata-data-4
        persistentVolumeClaim:
          claimName: netdata-data-4
      - name: netdata-data-5
        persistentVolumeClaim:
          claimName: netdata-data-5
      - name: netdata-data-6
        persistentVolumeClaim:
          claimName: netdata-data-6
      - name: netdata-data-7
        persistentVolumeClaim:
          claimName: netdata-data-7
      - name: netdata-data-8
        persistentVolumeClaim:
          claimName: netdata-data-8
      - name: netdata-data-9
        persistentVolumeClaim:
          claimName: netdata-data-9
      - name: netdata-data-10
        persistentVolumeClaim:
          claimName: netdata-data-10
      - name: netdata-data-11
        persistentVolumeClaim:
          claimName: netdata-data-11