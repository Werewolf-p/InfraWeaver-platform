---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/VandaLpr/unRAID-CA-templates/master/templates/adminer.xml
# Category: Tools-Utilities
# Stars: 976 | Downloads: 395563164
apiVersion: apps/v1
kind: Deployment
metadata:
  name: adminer
  namespace: adminer
  labels:
    app.kubernetes.io/name: adminer
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: adminer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: adminer
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: adminer
          image: adminer
          env:
            - name: ADMINER_DESIGN
              value: "flat"
            - name: ADMINER_PLUGINS
              value: ""
          ports:
            - containerPort: 8080
              protocol: TCP
              name: webui-port
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            failureThreshold: 10
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            failureThreshold: 10
          # No volume mounts defined
          securityContext:
            runAsNonRoot: false
            allowPrivilegeEscalation: false
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      # No volumes defined
---
apiVersion: v1
kind: Service
metadata:
  name: adminer
  namespace: adminer
  labels:
    app.kubernetes.io/name: adminer
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: adminer
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: webui-port
  type: ClusterIP