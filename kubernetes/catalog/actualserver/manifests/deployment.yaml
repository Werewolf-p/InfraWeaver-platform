---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/hofq/docker-templates/main/actual-server.xml
# Category: Other
# Stars: 67 | Downloads: 25488290
apiVersion: apps/v1
kind: Deployment
metadata:
  name: actualserver
  namespace: actualserver
  labels:
    app.kubernetes.io/name: actualserver
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: actualserver
  template:
    metadata:
      labels:
        app.kubernetes.io/name: actualserver
    spec:
      containers:
        - name: actualserver
          image: actualbudget/actual-server
          # No environment variables defined
          ports:
            - containerPort: 5006
              protocol: TCP
              name: web-ui
          readinessProbe:
            tcpSocket:
              port: 5006
            initialDelaySeconds: 30
            failureThreshold: 10
          livenessProbe:
            tcpSocket:
              port: 5006
            initialDelaySeconds: 30
            failureThreshold: 10
          volumeMounts:
            - name: actualserver-data-0
              mountPath: "/data"
          securityContext:
            runAsNonRoot: false
            allowPrivilegeEscalation: false
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
      - name: actualserver-data-0
        persistentVolumeClaim:
          claimName: actualserver-data-0
---
apiVersion: v1
kind: Service
metadata:
  name: actualserver
  namespace: actualserver
  labels:
    app.kubernetes.io/name: actualserver
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: actualserver
  ports:
    - port: 5006
      targetPort: 5006
      protocol: TCP
      name: web-ui
  type: ClusterIP