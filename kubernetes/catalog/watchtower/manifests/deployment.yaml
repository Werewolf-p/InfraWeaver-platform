---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/Frooodle/unraid-templates/main/watchtower.xml
# Category: Other
# Stars: 1237 | Downloads: 8234192348
apiVersion: apps/v1
kind: Deployment
metadata:
  name: watchtower
  namespace: watchtower
  labels:
    app.kubernetes.io/name: watchtower
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: watchtower
  template:
    metadata:
      labels:
        app.kubernetes.io/name: watchtower
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: watchtower
          image: containrrr/watchtower
          args:
            - "--schedule"
            - "0 0 4 * * *"
            - "--cleanup"
          # No environment variables defined
          # No ports defined

          volumeMounts:
            - name: watchtower-data-0
              mountPath: "/var/run/docker.sock"
          securityContext:
            runAsNonRoot: false
            allowPrivilegeEscalation: false
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
      - name: watchtower-data-0
        persistentVolumeClaim:
          claimName: watchtower-data-0