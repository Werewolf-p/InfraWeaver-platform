---
# catalog.yaml — source definition for catalog apps
# This file is read by scripts/sync-catalog.sh to generate ArgoCD Applications.
#
# Catalog app: Excalidraw — self-hosted collaborative virtual whiteboard
# Access: excalidraw.int.rlservers.com (VPN-only, protected by Authentik forward-auth)
# SSO: Traefik forward-auth (stateless, no backend storage)

name: excalidraw
description: "Excalidraw — self-hosted collaborative whiteboard and diagram tool"
namespace: excalidraw

ingressroute:
  host: excalidraw.int.rlservers.com
  entrypoint: websecure
