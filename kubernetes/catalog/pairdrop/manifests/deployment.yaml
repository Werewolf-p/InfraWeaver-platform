---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/linuxserver/templates/main/unraid/pairdrop.xml
# Category: Tools-Utilities, Network-Web
# Stars: 30 | Downloads: 570149
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pairdrop
  namespace: pairdrop
  labels:
    app.kubernetes.io/name: pairdrop
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: pairdrop
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pairdrop
    spec:
      containers:
        - name: pairdrop
          image: lscr.io/linuxserver/pairdrop
          env:
            - name: RATE_LIMIT
              value: "false"
            - name: WS_FALLBACK
              value: "false"
            - name: RTC_CONFIG
              value: ""
            - name: DEBUG_MODE
              value: "false"
            - name: PUID
              value: "99"
            - name: PGID
              value: "100"
            - name: UMASK
              value: "022"
          ports:
            - containerPort: 3000
              protocol: TCP
              name: webui
          # No volume mounts defined
          securityContext:
            runAsNonRoot: false
            allowPrivilegeEscalation: false
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
      # No volumes defined
---
apiVersion: v1
kind: Service
metadata:
  name: pairdrop
  namespace: pairdrop
  labels:
    app.kubernetes.io/name: pairdrop
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: pairdrop
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: webui
  type: ClusterIP
