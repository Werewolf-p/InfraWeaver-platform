---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/fanningert/unraid-docker-templates/master/fanningert/Gitea.xml
# Category: Backup, Cloud, Productivity, Tools-Utilities
# Stars: 1042 | Downloads: 370783512
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitea
  namespace: gitea
  labels:
    app.kubernetes.io/name: gitea
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: gitea
  template:
    metadata:
      labels:
        app.kubernetes.io/name: gitea
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: gitea
          image: gitea/gitea
          # No environment variables defined
          ports:
            - containerPort: 22
              protocol: TCP
              name: git-over-ssh
            - containerPort: 3000
              protocol: TCP
              name: webui-and-git-o
          readinessProbe:
            tcpSocket:
              port: 22
            initialDelaySeconds: 30
            failureThreshold: 10
          livenessProbe:
            tcpSocket:
              port: 22
            initialDelaySeconds: 30
            failureThreshold: 10
          volumeMounts:
            - name: gitea-data-0
              mountPath: "/data"
          securityContext:
            runAsNonRoot: false
            allowPrivilegeEscalation: false
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
      volumes:
      - name: gitea-data-0
        persistentVolumeClaim:
          claimName: gitea-data-0
---
apiVersion: v1
kind: Service
metadata:
  name: gitea
  namespace: gitea
  labels:
    app.kubernetes.io/name: gitea
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: gitea
  ports:
    - port: 22
      targetPort: 22
      protocol: TCP
      name: git-over-ssh
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: webui-and-git-o
  type: ClusterIP