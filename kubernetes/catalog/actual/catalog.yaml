---
# catalog.yaml — source definition for catalog apps
# This file is read by scripts/sync-catalog.sh to generate ArgoCD Applications.
#
# Catalog app: Actual Budget — self-hosted personal finance / budgeting tool
# Access: actual.int.rlservers.com (VPN-only, protected by Authentik forward-auth)
# SSO: Traefik forward-auth (Actual has no native OIDC; uses its own user accounts)

name: actual
description: "Actual Budget — self-hosted personal finance tool with envelope budgeting"
namespace: actual

ingressroute:
  host: actual.int.rlservers.com
  entrypoint: websecure
