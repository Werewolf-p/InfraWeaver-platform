---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/A75G/docker-templates/master/templates/deprecated/cyberchef.xml
# Category: Productivity, Tools-Utilities
# Stars: 53 | Downloads: 11445241
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cyberchef
  namespace: cyberchef
  labels:
    app.kubernetes.io/name: cyberchef
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cyberchef
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cyberchef
    spec:
      containers:
        - name: cyberchef
          image: mpepping/cyberchef:latest
          # No environment variables defined
          ports:
            - containerPort: 8000
              protocol: TCP
              name: port
          # No volume mounts defined
          securityContext:
            runAsNonRoot: false
            allowPrivilegeEscalation: false
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
      # No volumes defined
---
apiVersion: v1
kind: Service
metadata:
  name: cyberchef
  namespace: cyberchef
  labels:
    app.kubernetes.io/name: cyberchef
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: cyberchef
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: port
  type: ClusterIP
