---
# catalog.yaml — ntfy
# Defined but not enabled by default. Add 'ntfy' to catalog.enabled in platform.yaml to deploy.
# Access: ntfy.int.rlservers.com (VPN-only, protected by Authentik forward-auth)
# SSO: Traefik forward-auth (open topic access or forward-auth gate)

name: ntfy
description: "ntfy — simple HTTP-based pub/sub push notification service"
namespace: ntfy

ingressroute:
  host: ntfy.int.rlservers.com
  entrypoint: websecure
