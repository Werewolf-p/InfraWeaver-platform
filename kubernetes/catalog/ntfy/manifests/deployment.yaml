---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/Kostecki/unraid-ca-templates/main/templates/ntfy.xml
# Category: Tools-Utilities
# Stars: 103 | Downloads: 15386780
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ntfy
  namespace: ntfy
  labels:
    app.kubernetes.io/name: ntfy
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: ntfy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ntfy
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: ntfy
          image: binwiederhier/ntfy
          args:
            - "serve"
          env:
            - name: NTFY_BASE_URL
              value: "https://ntfy.israndom.win"
            - name: NTFY_WEB_ROOT
              value: "app"
            - name: NTFY_UPSTREAM_BASE_URL
              value: "https://ntfy.sh"
            - name: NTFY_CACHE_FILE
              value: "/config/cache.db"
            - name: NTFY_CACHE_DURATION
              value: "12h"
            - name: NTFY_AUTH_FILE
              value: "/config/user.db"
            - name: NTFY_AUTH_DEFAULT_ACCESS
              value: "deny-all"
            - name: NTFY_ATTACHMENT_CACHE_DIR
              value: "/config/attachments"
            - name: NTFY_ATTACHMENT_TOTAL_SIZE_LIMIT
              value: "5G"
            - name: NTFY_ATTACHMENT_FILE_SIZE_LIMIT
              value: "15M"
            - name: NTFY_ATTACHMENT_EXPIRY_DURATION
              value: "3h"
            - name: NTFY_SMTP_SENDER_ADDR
              value: ""
            - name: NTFY_SMTP_SENDER_USER
              value: ""
            - name: NTFY_SMTP_SENDER_PASS
              value: ""
            - name: NTFY_SMTP_SENDER_FROM
              value: ""
            - name: NTFY_SMTP_SERVER_ADDR_PREFIX
              value: ""
            - name: NTFY_LOG_LEVEL
              value: "info"
          ports:
            - containerPort: 80
              protocol: TCP
              name: host-port-1
          readinessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 30
            failureThreshold: 10
          livenessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 30
            failureThreshold: 10
          volumeMounts:
            - name: ntfy-data-0
              mountPath: "/config"
          securityContext:
            runAsNonRoot: false
            allowPrivilegeEscalation: false
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
      - name: ntfy-data-0
        persistentVolumeClaim:
          claimName: ntfy-data-0
---
apiVersion: v1
kind: Service
metadata:
  name: ntfy
  namespace: ntfy
  labels:
    app.kubernetes.io/name: ntfy
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: ntfy
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: host-port-1
  type: ClusterIP