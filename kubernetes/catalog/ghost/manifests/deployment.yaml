---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/BB-BenBridges/docker-templates/master/cheesemarathon/ghost.xml
# Category: Network-Web
# Stars: 1807 | Downloads: 375408865
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ghost
  namespace: ghost
  labels:
    app.kubernetes.io/name: ghost
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: ghost
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ghost
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: ghost
          image: ghost
          env:
            - name: database__connection__host
              value: ""
            - name: database__connection__user
              value: "ghost"
            - name: database__connection__password
              valueFrom:
                secretKeyRef:
                  name: database-password-secret
                  key: value
            - name: database__connection__database
              value: "ghost"
            - name: url
              value: ""
            - name: mail__transport
              value: "SMTP"
            - name: mail__options__service
              value: "Mailgun"
            - name: mail__options__auth__user
              value: ""
            - name: mail__options__auth__pass
              value: ""
            - name: database__client
              value: "mysql"
            - name: NODE_ENV
              value: "production"
          ports:
            - containerPort: 2368
              protocol: TCP
              name: web
          startupProbe:
            tcpSocket:
              port: 2368
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 120
          readinessProbe:
            tcpSocket:
              port: 2368
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 2368
            periodSeconds: 30
            failureThreshold: 3
          volumeMounts:
            - name: ghost-data-0
              mountPath: "/var/lib/ghost/content"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
      - name: ghost-data-0
        persistentVolumeClaim:
          claimName: ghost-data-0
---
apiVersion: v1
kind: Service
metadata:
  name: ghost
  namespace: ghost
  labels:
    app.kubernetes.io/name: ghost
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: ghost
  ports:
    - port: 2368
      targetPort: 2368
      protocol: TCP
      name: web
  type: ClusterIP