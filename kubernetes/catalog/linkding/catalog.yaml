---
# catalog.yaml — linkding
# Defined but not enabled by default. Add 'linkding' to catalog.enabled in platform.yaml to deploy.
# Access: linkding.int.rlservers.com (VPN-only, protected by Authentik forward-auth)
# SSO: Traefik forward-auth (linkding has built-in user auth; seed secret/platform/linkding in OpenBao)

name: linkding
description: "linkding — self-hosted bookmark manager with tagging and search"
namespace: linkding

ingressroute:
  host: linkding.int.rlservers.com
  entrypoint: websecure
