---
# catalog.yaml — navidrome
# Defined but not enabled by default. Add 'navidrome' to catalog.enabled in platform.yaml to deploy.
# Access: navidrome.int.rlservers.com (VPN-only, protected by Authentik forward-auth)
# SSO: Traefik forward-auth (Navidrome supports Last.fm/Subsonic clients; no native OIDC)

name: navidrome
description: "Navidrome — self-hosted music streaming server compatible with Subsonic/Airsonic clients"
namespace: navidrome

ingressroute:
  host: navidrome.int.rlservers.com
  entrypoint: websecure
