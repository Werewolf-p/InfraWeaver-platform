---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/junkerderprovinz/standardnotes-webui/main/templates/standardnotes-webui.xml
# Category: Productivity, Cloud
# Stars: 0 | Downloads: 0
apiVersion: apps/v1
kind: Deployment
metadata:
  name: standardnotes-webui
  namespace: standardnotes-webui
  labels:
    app.kubernetes.io/name: standardnotes-webui
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: standardnotes-webui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: standardnotes-webui
    spec:
      containers:
        - name: standardnotes-webui
          image: standardnotes/web:latest
          # No environment variables defined
          ports:
            - containerPort: 80
              protocol: TCP
              name: webui-port
          readinessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 30
            failureThreshold: 10
          livenessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 30
            failureThreshold: 10
          # No volume mounts defined
          securityContext:
            runAsNonRoot: false
            allowPrivilegeEscalation: false
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      # No volumes defined
---
apiVersion: v1
kind: Service
metadata:
  name: standardnotes-webui
  namespace: standardnotes-webui
  labels:
    app.kubernetes.io/name: standardnotes-webui
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: standardnotes-webui
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: webui-port
  type: ClusterIP