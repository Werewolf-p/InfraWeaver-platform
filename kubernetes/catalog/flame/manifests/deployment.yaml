---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/pawelmalak/unraid-templates/master/templates/flame.xml
# Category: Tools-Utilities, Network-Web
# Stars: 127 | Downloads: 10087518
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flame
  namespace: flame
  labels:
    app.kubernetes.io/name: flame
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: flame
  template:
    metadata:
      labels:
        app.kubernetes.io/name: flame
    spec:
      enableServiceLinks: false
      securityContext:
        fsGroup: 1000
      containers:
        - name: flame
          image: pawelmalak/flame
          env:
            - name: PASSWORD
              value: "flame_password"
          ports:
            - containerPort: 5005
              protocol: TCP
              name: host-port-1
          startupProbe:
            tcpSocket:
              port: 5005
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 120
          readinessProbe:
            tcpSocket:
              port: 5005
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 5005
            periodSeconds: 30
            failureThreshold: 3
          volumeMounts:
            - name: flame-data-0
              mountPath: "/app/data"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
      - name: flame-data-0
        persistentVolumeClaim:
          claimName: flame-data-0
---
apiVersion: v1
kind: Service
metadata:
  name: flame
  namespace: flame
  labels:
    app.kubernetes.io/name: flame
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: flame
  ports:
    - port: 5005
      targetPort: 5005
      protocol: TCP
      name: host-port-1
  type: ClusterIP