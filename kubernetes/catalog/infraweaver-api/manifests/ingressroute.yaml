# IngressRoute for api.int.rlservers.com is managed in:
#   kubernetes/platform/external-routes/manifests/10-routes-vpn-only.yaml
#
# It uses the centralized routing pattern with netbird-vpn-only middleware.
# WebSocket connections (wss://) for node agents are routed through the same IngressRoute.
# URL: https://api.int.rlservers.com (internal/VPN only)
