---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/juusujanar/unraid-templates/master/PostgreSQL17.xml
# Category: Network-Management, Productivity, Tools-Utilities
# Stars: 14883 | Downloads: 10537490659
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgresql17
  namespace: postgresql17
  labels:
    app.kubernetes.io/name: postgresql17
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: postgresql17
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgresql17
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: postgresql17
          image: postgres:17
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-password-secret
                  key: value
            - name: POSTGRES_USER
              value: ""
            - name: POSTGRES_DB
              value: ""
          ports:
            - containerPort: 5432
              protocol: TCP
              name: postgresql-ac
          startupProbe:
            tcpSocket:
              port: 5432
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 120
          readinessProbe:
            tcpSocket:
              port: 5432
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 5432
            periodSeconds: 30
            failureThreshold: 3
          volumeMounts:
            - name: postgresql17-data-0
              mountPath: "/var/lib/postgresql/data"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
      volumes:
      - name: postgresql17-data-0
        persistentVolumeClaim:
          claimName: postgresql17-data-0
---
apiVersion: v1
kind: Service
metadata:
  name: postgresql17
  namespace: postgresql17
  labels:
    app.kubernetes.io/name: postgresql17
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: postgresql17
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgresql-ac
  type: ClusterIP