---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/VandaLpr/unRAID-CA-templates/master/templates/prometheus.xml
# Category: Productivity
# Stars: 2060 | Downloads: 1958961335
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
  namespace: prometheus
  labels:
    app.kubernetes.io/name: prometheus
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: prometheus
  template:
    metadata:
      labels:
        app.kubernetes.io/name: prometheus
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: prometheus
          image: prom/prometheus
          # No environment variables defined
          ports:
            - containerPort: 9090
              protocol: TCP
              name: port
          startupProbe:
            tcpSocket:
              port: 9090
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 120
          readinessProbe:
            tcpSocket:
              port: 9090
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 9090
            periodSeconds: 30
            failureThreshold: 3
          volumeMounts:
            - name: prometheus-data-0
              mountPath: "/prometheus"
            - name: prometheus-data-1
              mountPath: "/etc/prometheus/"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
      - name: prometheus-data-0
        persistentVolumeClaim:
          claimName: prometheus-data-0
      - name: prometheus-data-1
        persistentVolumeClaim:
          claimName: prometheus-data-1
---
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: prometheus
  labels:
    app.kubernetes.io/name: prometheus
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: prometheus
  ports:
    - port: 9090
      targetPort: 9090
      protocol: TCP
      name: port
  type: ClusterIP