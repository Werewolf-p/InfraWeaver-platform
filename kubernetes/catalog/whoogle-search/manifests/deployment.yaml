---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/FoxxMD/unraid-docker-templates/master/foxxmd/whoogle-search.xml
# Category: Cloud, Network-Web
# Stars: 45 | Downloads: 19760063
apiVersion: apps/v1
kind: Deployment
metadata:
  name: whoogle-search
  namespace: whoogle-search
  labels:
    app.kubernetes.io/name: whoogle-search
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: whoogle-search
  template:
    metadata:
      labels:
        app.kubernetes.io/name: whoogle-search
    spec:
      enableServiceLinks: false
      securityContext:
        fsGroup: 1000
      containers:
        - name: whoogle-search
          image: benbusby/whoogle-search
          env:
            - name: HTTPS_ONLY
              value: ""
            - name: EXPOSE_PORT
              value: ""
          ports:
            - containerPort: 5000
              protocol: TCP
              name: web-ui
          startupProbe:
            tcpSocket:
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 120
          readinessProbe:
            tcpSocket:
              port: 5000
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 5000
            periodSeconds: 30
            failureThreshold: 3
          volumeMounts:
            - name: whoogle-search-data-0
              mountPath: "/config"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
      - name: whoogle-search-data-0
        persistentVolumeClaim:
          claimName: whoogle-search-data-0
---
apiVersion: v1
kind: Service
metadata:
  name: whoogle-search
  namespace: whoogle-search
  labels:
    app.kubernetes.io/name: whoogle-search
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: whoogle-search
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: web-ui
  type: ClusterIP