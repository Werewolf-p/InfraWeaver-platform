---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/linuxserver/templates/main/unraid/deprecated/overseerr.xml
# Category: MediaServer-Video, Tools-Utilities
# Stars: 191 | Downloads: 11951935
apiVersion: apps/v1
kind: Deployment
metadata:
  name: overseerr
  namespace: overseerr
  labels:
    app.kubernetes.io/name: overseerr
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: overseerr
  template:
    metadata:
      labels:
        app.kubernetes.io/name: overseerr
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: overseerr
          image: lscr.io/linuxserver/overseerr
          env:
            - name: PUID
              value: "99"
            - name: PGID
              value: "100"
            - name: UMASK
              value: "022"
            - name: TZ
              value: "UTC"
          ports:
            - containerPort: 5055
              protocol: TCP
              name: webui
          startupProbe:
            tcpSocket:
              port: 5055
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 120
          readinessProbe:
            tcpSocket:
              port: 5055
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 5055
            periodSeconds: 30
            failureThreshold: 3
          volumeMounts:
            - name: overseerr-data-0
              mountPath: "/config"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
      - name: overseerr-data-0
        persistentVolumeClaim:
          claimName: overseerr-data-0
---
apiVersion: v1
kind: Service
metadata:
  name: overseerr
  namespace: overseerr
  labels:
    app.kubernetes.io/name: overseerr
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: overseerr
  ports:
    - port: 5055
      targetPort: 5055
      protocol: TCP
      name: webui
  type: ClusterIP