---
# Generated by InfraWeaver Community Apps from Unraid AppFeed
# Source: https://raw.githubusercontent.com/b42n1/unraid-templates/main/templates/memos.xml
# Category: Productivity, Tools-Utilities
# Stars: 225 | Downloads: 7929454
apiVersion: apps/v1
kind: Deployment
metadata:
  name: memos
  namespace: memos
  labels:
    app.kubernetes.io/name: memos
    app.kubernetes.io/managed-by: infraweaver
    infraweaver.io/source: community-apps
    infraweaver.io/tier: simple
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: memos
  template:
    metadata:
      labels:
        app.kubernetes.io/name: memos
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: memos
          image: neosmemo/memos:latest
          # No environment variables defined
          ports:
            - containerPort: 5230
              protocol: TCP
              name: port
          readinessProbe:
            tcpSocket:
              port: 5230
            initialDelaySeconds: 30
            failureThreshold: 10
          livenessProbe:
            tcpSocket:
              port: 5230
            initialDelaySeconds: 30
            failureThreshold: 10
          volumeMounts:
            - name: memos-data-0
              mountPath: "/var/opt/memos"
          securityContext:
            runAsNonRoot: false
            allowPrivilegeEscalation: false
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
      - name: memos-data-0
        persistentVolumeClaim:
          claimName: memos-data-0
---
apiVersion: v1
kind: Service
metadata:
  name: memos
  namespace: memos
  labels:
    app.kubernetes.io/name: memos
    infraweaver.io/source: community-apps
spec:
  selector:
    app.kubernetes.io/name: memos
  ports:
    - port: 5230
      targetPort: 5230
      protocol: TCP
      name: port
  type: ClusterIP