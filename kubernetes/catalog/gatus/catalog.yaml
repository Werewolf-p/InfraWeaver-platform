---
# catalog.yaml — Gatus status page
# GitOps-managed, config-driven endpoint health monitoring with Discord alerts
# Access: status.int.${BASE_DOMAIN} (VPN-only)
# SSO: Authentik forward-auth via Traefik middleware

name: gatus
description: "Gatus — GitOps-managed status page with endpoint health monitoring and Discord alerts"
namespace: gatus

ingressroute:
  host: status.int.${BASE_DOMAIN}
  entrypoint: websecure
replicas: "1"
