---
# IngressRoutes for *.int.rlservers.com — private/VPN-only services.
#
# Security model (two layers):
#   Layer 1 — Private IP boundary:
#     Cloudflare DNS-only wildcard: *.int.rlservers.com → 10.10.0.200 (no proxy)
#     10.10.0.200 is a private VLAN3 IP — unreachable from the public internet
#
#   Layer 2 — IP allowlist via netbird-vpn-only middleware:
#     Only traffic from the NetBird routing peer (10.10.0.10) is allowed.
#     VPN clients route 10.10.0.0/24 via the netbird-router-vlan3 VM (VM 9200),
#     which masquerades their traffic — Traefik sees source = 10.10.0.10.
#     Direct VLAN3 hosts (10.25.0.x bridged, 10.10.0.85 runner) are BLOCKED.
#
# TLS: int-rlservers-com-tls (wildcard cert via letsencrypt-cloudflare DNS-01)

# ── NetBird Management gRPC (int) ─────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: netbird-management-grpc-int
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`netbird.int.rlservers.com`) && PathPrefix(`/management.ManagementService`)
      kind: Rule
      priority: 50
      middlewares:
        - name: netbird-vpn-only
          namespace: traefik
      services:
        - name: netbird-management
          port: 33073
          scheme: h2c
  tls:
    secretName: int-rlservers-com-tls
---
# ── NetBird Signal gRPC (int) ─────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: netbird-signal-grpc-int
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`netbird.int.rlservers.com`) && (PathPrefix(`/signalexchange.SignalExchange`) || PathPrefix(`/grpc.reflection`))
      kind: Rule
      priority: 50
      middlewares:
        - name: netbird-vpn-only
          namespace: traefik
      services:
        - name: netbird-signal
          port: 10000
          scheme: h2c
  tls:
    secretName: int-rlservers-com-tls
---
# ── NetBird Relay (int) ───────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: netbird-relay-int
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`netbird.int.rlservers.com`) && PathPrefix(`/relay`)
      kind: Rule
      priority: 40
      middlewares:
        - name: netbird-vpn-only
          namespace: traefik
      services:
        - name: netbird-relay
          port: 443
          serversTransport: insecure-skip-verify
  tls:
    secretName: int-rlservers-com-tls
---
# ── NetBird Management API + WS proxy (int) ───────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: netbird-management-api-int
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`netbird.int.rlservers.com`) && (PathPrefix(`/api`) || PathPrefix(`/ws-proxy/management`))
      kind: Rule
      priority: 30
      middlewares:
        - name: netbird-vpn-only
          namespace: traefik
      services:
        - name: netbird-management
          port: 80
  tls:
    secretName: int-rlservers-com-tls
---
# ── NetBird Dashboard (int — VPN only, catch-all) ────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: netbird-dashboard-int
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`netbird.int.rlservers.com`)
      kind: Rule
      priority: 10
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: netbird-vpn-only
          namespace: traefik
      services:
        - name: netbird-dashboard
          port: 80
  tls:
    secretName: int-rlservers-com-tls
---
# ── ArgoCD ────────────────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-int
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`argocd.int.rlservers.com`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: netbird-vpn-only
          namespace: traefik
      services:
        - name: argocd-server
          port: 80
  tls:
    secretName: int-rlservers-com-tls
---
# ── Grafana ───────────────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana-int
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`grafana.int.rlservers.com`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: netbird-vpn-only
          namespace: traefik
      services:
        - name: grafana-apps
          port: 80
  tls:
    secretName: int-rlservers-com-tls
---
# ── Longhorn ──────────────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: longhorn-int
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`longhorn.int.rlservers.com`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: netbird-vpn-only
          namespace: traefik
        - name: forward-auth
          namespace: traefik
      services:
        - name: longhorn-frontend
          port: 80
  tls:
    secretName: int-rlservers-com-tls
---
# ── OpenBao (Vault) ───────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: openbao-int
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`openbao.int.rlservers.com`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: netbird-vpn-only
          namespace: traefik
      services:
        - name: openbao
          port: 8200
  tls:
    secretName: int-rlservers-com-tls
