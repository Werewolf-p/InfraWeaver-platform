---
# IngressRoutes for *.int.${BASE_DOMAIN} — private/VPN-only services.
#
# Security model (two layers):
#   Layer 1 — Private IP boundary:
#     Cloudflare DNS-only wildcard: *.int.${BASE_DOMAIN} → ${METALLB_TRAEFIK_VIP} (no proxy)
#     ${METALLB_TRAEFIK_VIP} is a private VLAN3 IP — unreachable from the public internet
#
#   Layer 2 — IP allowlist via netbird-vpn-only middleware:
#     Only traffic from the NetBird routing peer (10.10.0.10) is allowed.
#     VPN clients route 10.10.0.0/24 via the netbird-router-vlan3 VM (VM 9200),
#     which masquerades their traffic — Traefik sees source = 10.10.0.10.
#     Direct VLAN3 hosts (10.25.0.x bridged, 10.10.0.85 runner) are BLOCKED.
#
# TLS: platform-int-wildcard-tls (wildcard cert via letsencrypt-cloudflare DNS-01)

# ── NetBird Management gRPC (int) ─────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: netbird-management-grpc-int
  namespace: traefik
  labels:
    infraweaver.io/access-tier: vpn
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`netbird.int.${BASE_DOMAIN}`) && PathPrefix(`/management.ManagementService`)
      kind: Rule
      priority: 50
      middlewares:
        - name: internal-only
          namespace: traefik
      services:
        - name: netbird-management
          port: 33073
          scheme: h2c
  tls:
    secretName: platform-int-wildcard-tls
---
# ── NetBird Signal gRPC (int) ─────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: netbird-signal-grpc-int
  namespace: traefik
  labels:
    infraweaver.io/access-tier: vpn
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`netbird.int.${BASE_DOMAIN}`) && (PathPrefix(`/signalexchange.SignalExchange`) || PathPrefix(`/grpc.reflection`))
      kind: Rule
      priority: 50
      middlewares:
        - name: internal-only
          namespace: traefik
      services:
        - name: netbird-signal
          port: 10000
          scheme: h2c
  tls:
    secretName: platform-int-wildcard-tls
---
# ── NetBird Relay (int) ───────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: netbird-relay-int
  namespace: traefik
  labels:
    infraweaver.io/access-tier: vpn
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`netbird.int.${BASE_DOMAIN}`) && PathPrefix(`/relay`)
      kind: Rule
      priority: 40
      middlewares:
        - name: internal-only
          namespace: traefik
      services:
        - name: netbird-relay
          port: 443
          serversTransport: insecure-skip-verify
  tls:
    secretName: platform-int-wildcard-tls
---
# ── NetBird Management API + WS proxy (int) ───────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: netbird-management-api-int
  namespace: traefik
  labels:
    infraweaver.io/access-tier: vpn
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`netbird.int.${BASE_DOMAIN}`) && (PathPrefix(`/api`) || PathPrefix(`/ws-proxy/management`))
      kind: Rule
      priority: 30
      middlewares:
        - name: internal-only
          namespace: traefik
      services:
        - name: netbird-management
          port: 80
  tls:
    secretName: platform-int-wildcard-tls
---
# ── NetBird Dashboard (int — VPN only, catch-all) ────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: netbird-dashboard-int
  namespace: traefik
  labels:
    infraweaver.io/access-tier: vpn
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`netbird.int.${BASE_DOMAIN}`)
      kind: Rule
      priority: 10
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: internal-only
          namespace: traefik
      services:
        - name: netbird-dashboard
          port: 80
  tls:
    secretName: platform-int-wildcard-tls
---
# ── ArgoCD ────────────────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-int
  namespace: traefik
  labels:
    infraweaver.io/access-tier: vpn
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`argocd.int.${BASE_DOMAIN}`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: internal-only
          namespace: traefik
      services:
        - name: argocd-server
          port: 80
  tls:
    secretName: platform-int-wildcard-tls
---
# ── Grafana ───────────────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana-int
  namespace: traefik
  labels:
    infraweaver.io/access-tier: vpn
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`grafana.int.${BASE_DOMAIN}`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: internal-only
          namespace: traefik
      services:
        - name: grafana-apps
          port: 80
  tls:
    secretName: platform-int-wildcard-tls
---
# ── Longhorn ──────────────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: longhorn-int
  namespace: traefik
  labels:
    infraweaver.io/access-tier: vpn
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`longhorn.int.${BASE_DOMAIN}`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: internal-only
          namespace: traefik
        - name: forward-auth
          namespace: traefik
      services:
        - name: longhorn-frontend
          port: 80
  tls:
    secretName: platform-int-wildcard-tls
---
# ── OpenBao (Vault) ───────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: openbao-int
  namespace: traefik
  labels:
    infraweaver.io/access-tier: vpn
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`openbao.int.${BASE_DOMAIN}`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: internal-only
          namespace: traefik
      services:
        - name: openbao
          port: 8200
  tls:
    secretName: platform-int-wildcard-tls
---
# ── InfraWeaver Console ───────────────────────────────────────────────────────
# Next.js management UI — handles its own Authentik OAuth2 auth internally.
# Forward-auth NOT used (requires Proxy Provider; app uses OAuth2 provider instead).
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: infraweaver-console-int
  namespace: traefik
  labels:
    infraweaver.io/access-tier: vpn
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`infraweaver.int.${BASE_DOMAIN}`)
      kind: Rule
      middlewares:
        - name: internal-only
          namespace: traefik
        - name: secure-headers
          namespace: traefik
      services:
        - name: infraweaver-console
          port: 3000
  tls:
    secretName: platform-int-wildcard-tls
---
# InfraWeaver Hub API — internal only, VPN-protected
# WebSocket (wss://) is supported via Traefik's passthrough
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: infraweaver-api-int
  namespace: traefik
  labels:
    infraweaver.io/access-tier: vpn
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`api.int.${BASE_DOMAIN}`)
      kind: Rule
      middlewares:
        - name: internal-only
          namespace: traefik
        - name: secure-headers
          namespace: traefik
      services:
        - name: infraweaver-api
          namespace: infraweaver-console
          port: 3001
  tls:
    secretName: platform-int-wildcard-tls
