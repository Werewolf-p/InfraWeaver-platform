---
# IngressRoutes for public-facing services (no IP restriction)

# ── WordPress: rlservers.com ──────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: wp-rlservers
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`rlservers.com`) || Host(`www.rlservers.com`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
      services:
        - name: bm-wp-rlservers
          port: 443
          scheme: https
          serversTransport: insecure-skip-verify
  tls:
    secretName: platform-wildcard-int-tls
---
# ── degoudentijd.rlservers.com ────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: degoudentijd
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`degoudentijd.rlservers.com`) || Host(`www.degoudentijd.rlservers.com`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: add-https-headers
          namespace: traefik
      services:
        - name: bm-degoudentijd
          port: 80
  tls:
    secretName: platform-wildcard-int-tls
---
# ── feestinhetdonker.rlservers.com ────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: feest
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`feestinhetdonker.rlservers.com`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: add-https-headers
          namespace: traefik
      services:
        - name: bm-feest
          port: 5000
  tls:
    secretName: platform-wildcard-int-tls
---
# ── marcel.rlservers.com ──────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: marcel
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`marcel.rlservers.com`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: add-https-headers
          namespace: traefik
      services:
        - name: bm-marcel
          port: 5000
  tls:
    secretName: platform-wildcard-int-tls
---
# ── Keycloak ──────────────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: keycloak
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`keycloak.rlservers.com`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
      services:
        - name: bm-keycloak
          port: 7080
  tls:
    secretName: platform-wildcard-int-tls
---
# ── AWX ───────────────────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: awx
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`awx.rlservers.com`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
      services:
        - name: bm-awx
          port: 30750
  tls:
    secretName: platform-wildcard-int-tls
---
# ── Bitwarden / Vaultwarden (public) ─────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: bitwarden
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`bitwarden.rlservers.com`) || Host(`vaultwarden.rlservers.com`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
      services:
        - name: bm-bitwarden
          port: 30032
  tls:
    secretName: rlservers-com-wildcard-tls
---
# ── GitLab ────────────────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: gitlab
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`gitlab.rlservers.com`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
      services:
        - name: bm-gitlab
          port: 80
  tls:
    secretName: platform-wildcard-int-tls
---
# ── GitLab Container Registry ─────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: gitlab-registry
  namespace: traefik
spec:
  entryPoints:
    - registry
  routes:
    - match: Host(`registry.gitlab.rlservers.com`)
      kind: Rule
      services:
        - name: bm-gitlab-registry
          port: 5005
  tls:
    secretName: rlservers-com-wildcard-tls
---
# ── Nextcloud ─────────────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: nextcloud
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`cloud.rlservers.com`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
      services:
        - name: bm-cloud
          port: 30027
          serversTransport: insecure-skip-verify
  tls:
    secretName: platform-wildcard-int-tls
---
# ── Teleport ──────────────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: teleport
  namespace: traefik
spec:
  entryPoints:
    - websecure
    - teleport-proxy
  routes:
    - match: Host(`teleport.rlservers.com`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
      services:
        - name: bm-teleport
          port: 3080
          serversTransport: insecure-skip-verify
  tls:
    secretName: platform-wildcard-int-tls
---
# ── Container Registry ────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: registry
  namespace: traefik
spec:
  entryPoints:
    - registry
  routes:
    - match: Host(`registry.rlservers.com`)
      kind: Rule
      services:
        - name: bm-registry
          port: 80
  tls:
    secretName: rlservers-com-wildcard-tls
---
# ── yonavaarwater.nl ──────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: yonavaarwater
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`yonavaarwater.nl`) || Host(`www.yonavaarwater.nl`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: add-https-headers
          namespace: traefik
      services:
        - name: bm-wp-yonava
          port: 80
  tls:
    secretName: yonavaarwater-nl-tls
---
# ── ot.yonavaarwater.nl (with forward-auth) ───────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: yonavaarwater-ot
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`ot.yonavaarwater.nl`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: forward-auth
          namespace: traefik
      services:
        - name: bm-feest
          port: 5000
  tls:
    secretName: yonavaarwater-nl-tls
---
# ── zonnevaarwater.nl ─────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: zonnevaarwater
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`zonnevaarwater.nl`) || Host(`www.zonnevaarwater.nl`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: add-https-headers
          namespace: traefik
      services:
        - name: bm-wp-zonneva
          port: 80
  tls:
    secretName: zonnevaarwater-nl-tls
---
# ── waterdance.nl ─────────────────────────────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: waterdance
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`waterdance.nl`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
      services:
        - name: bm-waterdance
          port: 443
          scheme: https
          serversTransport: insecure-skip-verify
  tls:
    secretName: waterdance-nl-tls
