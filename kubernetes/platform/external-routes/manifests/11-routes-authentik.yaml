---
# CORS middleware for auth.infraweaver.local
# Authentik handles CORS for application-specific OAuth2 endpoints (token, authorize)
# but NOT for the global revocation endpoint (/application/o/revoke/) or userinfo.
# This middleware adds CORS headers for those missing endpoints.
# Traefik replaces any existing Access-Control-Allow-Origin (no duplicates).
# Dashboard is public at netbird.infraweaver.local.
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: cors-authentik
  namespace: traefik
spec:
  headers:
    customResponseHeaders:
      Access-Control-Allow-Origin: "https://netbird.infraweaver.local"
      Access-Control-Allow-Methods: "GET, POST, OPTIONS, HEAD"
      Access-Control-Allow-Headers: "Authorization, Content-Type, Accept"
      Access-Control-Allow-Credentials: "true"
      Access-Control-Max-Age: "86400"
---
# Authentik — IngressRoute at auth.infraweaver.local
# No VPN restriction: users need to reach Authentik to log in via SSO
# before connecting to services behind the VPN middleware.
# Uses individual cert (platform-auth-tls) to avoid rate limit dependency on main bundle.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: authentik
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`auth.infraweaver.local`)
      kind: Rule
      middlewares:
        - name: secure-headers
          namespace: traefik
        - name: cors-authentik
          namespace: traefik
        - name: rate-limit-auth      # brute-force protection on public auth endpoint
          namespace: traefik
      services:
        - name: authentik-server
          namespace: authentik
          port: 80
  tls:
    secretName: platform-wildcard-tls

