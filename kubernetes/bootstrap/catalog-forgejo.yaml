---
# catalog-forgejo.yaml — Auto-generated by scripts/sync-catalog.sh
# Source: kubernetes/catalog/forgejo/catalog.yaml
# Enable/disable this app in platform.yaml under catalog.enabled.
# DO NOT edit manually — changes will be overwritten on next sync.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog-forgejo
  namespace: argocd
  labels:
    infraweaver.io/type: catalog-app
  annotations:
    infraweaver.io/description: "Forgejo — community-governed Git forge (Gitea fork) with Authentik native OIDC"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  sources:
    - repoURL: https://github.com/Werewolf-p/InfraWeaver-platform.git
      targetRevision: HEAD
      ref: values
    - repoURL: "https://dl.gitea.com/charts/"
      targetRevision: "10.*"
      chart: "gitea"
      helm:
        releaseName: "forgejo"
        valueFiles:
          - "$values/kubernetes/catalog/forgejo/values.yaml"
  destination:
    server: https://kubernetes.default.svc
    namespace: "forgejo"
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - kind: Secret
      jqPathExpressions:
        - '.data["admin-password"]'
        - '.data["admin-user"]'
