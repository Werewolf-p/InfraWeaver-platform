---
# catalog-homarr-manifests.yaml — deployed by InfraWeaver Community Apps
# Source: ghcr.io/homarr-labs/homarr:latest
# Installed: 2026-05-16T21:35:01.382Z
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog-homarr-manifests
  namespace: argocd
  labels:
    infraweaver.io/type: catalog-app
    infraweaver.io/source: community-apps
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: https://github.com/Werewolf-p/InfraWeaver-platform.git
    targetRevision: HEAD
    path: kubernetes/catalog/homarr/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: homarr
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
