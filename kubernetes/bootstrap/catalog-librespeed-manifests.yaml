---
# catalog-librespeed-manifests.yaml — deployed by InfraWeaver Community Apps
# Source: ghcr.io/librespeed/speedtest
# Installed: 2026-05-13T19:33:16.568Z
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog-librespeed-manifests
  namespace: argocd
  labels:
    infraweaver.io/type: catalog-app
    infraweaver.io/source: community-apps
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: https://github.com/Werewolf-p/InfraWeaver-platform.git
    targetRevision: HEAD
    path: kubernetes/catalog/librespeed/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: librespeed
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
