---
# catalog-infraweaver-console-manifests.yaml — Auto-generated by scripts/sync-catalog.sh
# Source: kubernetes/catalog/infraweaver-console/manifests/
# DO NOT edit manually — changes will be overwritten on next sync.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog-infraweaver-console-manifests
  namespace: argocd
  labels:
    infraweaver.io/type: catalog-app
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: https://github.com/Werewolf-p/InfraWeaver-platform.git
    targetRevision: HEAD
    path: kubernetes/catalog/infraweaver-console/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: "infraweaver-console"
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - group: external-secrets.io
      kind: ExternalSecret
      jsonPointers:
        - /status
        - /metadata/finalizers
        - /spec/target/creationPolicy
        - /spec/target/deletionPolicy
      jqPathExpressions:
        - '.spec.data[].remoteRef.conversionStrategy'
        - '.spec.data[].remoteRef.decodingStrategy'
        - '.spec.data[].remoteRef.metadataPolicy'
    - group: external-secrets.io
      kind: PushSecret
      jqPathExpressions:
        - '.spec.data[].match.remoteRef.conversionStrategy'
        - '.spec.data[].match.remoteRef.decodingStrategy'
        - '.spec.data[].match.remoteRef.metadataPolicy'
      jsonPointers:
        - /status
        - /metadata/finalizers
        - /metadata/annotations/kubectl.kubernetes.io~1last-applied-configuration
        - /metadata/annotations/reconcile
        - /spec/deletionPolicy
        - /spec/updatePolicy
        - /spec/template
        - /spec/selector
