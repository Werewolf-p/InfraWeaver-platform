---
# catalog-wallos-manifests.yaml — deployed by InfraWeaver Community Apps
# Source: bellamy/wallos:latest
# Installed: 2026-05-18T05:57:05.745Z
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog-wallos-manifests
  namespace: argocd
  labels:
    infraweaver.io/type: catalog-app
    infraweaver.io/source: community-apps
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: https://github.com/Werewolf-p/InfraWeaver-platform.git
    targetRevision: HEAD
    path: kubernetes/catalog/wallos/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: wallos
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
