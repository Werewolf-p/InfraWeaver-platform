---
# catalog-postgresql17-manifests.yaml — deployed by InfraWeaver Community Apps
# Source: postgres:17
# Installed: 2026-05-17T18:20:13.907Z
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog-postgresql17-manifests
  namespace: argocd
  labels:
    infraweaver.io/type: catalog-app
    infraweaver.io/source: community-apps
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: https://github.com/Werewolf-p/InfraWeaver-platform.git
    targetRevision: HEAD
    path: kubernetes/catalog/postgresql17/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: postgresql17
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
