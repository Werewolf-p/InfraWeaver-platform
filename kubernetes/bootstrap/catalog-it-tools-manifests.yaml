---
# catalog-it-tools-manifests.yaml — deployed by InfraWeaver Community Apps
# Source: ghcr.io/sharevb/it-tools:latest
# Installed: 2026-05-17T22:29:58.995Z
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog-it-tools-manifests
  namespace: argocd
  labels:
    infraweaver.io/type: catalog-app
    infraweaver.io/source: community-apps
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: https://github.com/Werewolf-p/InfraWeaver-platform.git
    targetRevision: HEAD
    path: kubernetes/catalog/it-tools/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: it-tools
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
