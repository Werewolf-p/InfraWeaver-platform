---
# catalog-linkding-manifests.yaml — deployed by InfraWeaver Community Apps
# Source: sissbruecker/linkding
# Installed: 2026-05-18T06:52:44.288Z
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog-linkding-manifests
  namespace: argocd
  labels:
    infraweaver.io/type: catalog-app
    infraweaver.io/source: community-apps
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: http://onedev.onedev.svc.cluster.local/InfraWeaver-platform
    targetRevision: HEAD
    path: kubernetes/catalog/linkding/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: linkding
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
