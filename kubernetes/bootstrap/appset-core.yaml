---
# appset-core.yaml — Mandatory ApplicationSet for kubernetes/core/ tier.
# This file is regenerated by scripts/sync-groups.sh — do not edit manually
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: platform-core
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=zero"]
  generators:
    - git:
        repoURL: ${GIT_REPO_URL}.git
        revision: HEAD
        files:
          - path: "kubernetes/core/*/application.yaml"
  template:
    metadata:
      name: '{{ index (splitList "/" .path.path) 1 }}-{{ index (splitList "/" .path.path) 2 }}'
      namespace: argocd
    spec:
      project: platform
      sources:
        - repoURL: ${GIT_REPO_URL}.git
          targetRevision: HEAD
          ref: values
        - repoURL: "{{ .repoURL }}"
          targetRevision: "{{ .targetRevision }}"
          chart: "{{ .chart }}"
          helm:
            releaseName: "{{ .releaseName }}"
            valueFiles:
              - "$values/{{ .path.path }}/values.yaml"
            parameters:
              - name: replicaCount
                value: '{{ if .replicas }}{{ .replicas }}{{ else }}1{{ end }}'
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{ .namespace }}"
      syncPolicy:
        automated:
          prune: false
          selfHeal: true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
        syncOptions:
          - CreateNamespace=true
          - '{{ if eq (default "true" .serverSideApply) "false" }}ServerSideApply=false{{ else }}ServerSideApply=true{{ end }}'
          - RespectIgnoreDifferences=true
      ignoreDifferences:
        - kind: Secret
          jqPathExpressions:
            - '.data["admin-password"]'
            - '.data["admin-user"]'
        - group: external-secrets.io
          kind: ExternalSecret
          jsonPointers:
            - /status
            - /metadata/finalizers
          jqPathExpressions:
            - '.spec.data[].remoteRef.conversionStrategy'
            - '.spec.data[].remoteRef.decodingStrategy'
            - '.spec.data[].remoteRef.metadataPolicy'
        - group: apps
          kind: StatefulSet
          jsonPointers:
            - /spec/persistentVolumeClaimRetentionPolicy
          managedFieldsManagers:
            - kube-controller-manager
