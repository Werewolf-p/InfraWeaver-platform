---
# catalog-flame-manifests.yaml — deployed by InfraWeaver Community Apps
# Source: pawelmalak/flame
# Installed: 2026-05-17T10:33:55.744Z
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog-flame-manifests
  namespace: argocd
  labels:
    infraweaver.io/type: catalog-app
    infraweaver.io/source: community-apps
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: https://github.com/Werewolf-p/InfraWeaver-platform.git
    targetRevision: HEAD
    path: kubernetes/catalog/flame/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: flame
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
