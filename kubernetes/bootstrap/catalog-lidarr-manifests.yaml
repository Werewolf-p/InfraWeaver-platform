---
# catalog-lidarr-manifests.yaml — deployed by InfraWeaver Community Apps
# Source: lscr.io/linuxserver/lidarr
# Installed: 2026-05-17T23:13:34.513Z
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog-lidarr-manifests
  namespace: argocd
  labels:
    infraweaver.io/type: catalog-app
    infraweaver.io/source: community-apps
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: http://onedev.onedev.svc.cluster.local/InfraWeaver-platform
    targetRevision: HEAD
    path: kubernetes/catalog/lidarr/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: lidarr
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
