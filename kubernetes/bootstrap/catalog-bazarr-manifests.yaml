---
# catalog-bazarr-manifests.yaml — deployed by InfraWeaver Community Apps
# Source: lscr.io/linuxserver/bazarr
# Installed: 2026-05-17T22:27:33.437Z
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog-bazarr-manifests
  namespace: argocd
  labels:
    infraweaver.io/type: catalog-app
    infraweaver.io/source: community-apps
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: https://github.com/Werewolf-p/InfraWeaver-platform.git
    targetRevision: HEAD
    path: kubernetes/catalog/bazarr/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: bazarr
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
