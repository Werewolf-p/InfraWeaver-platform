---
# catalog-jackett-manifests.yaml — deployed by InfraWeaver Community Apps
# Source: lscr.io/linuxserver/jackett
# Installed: 2026-05-17T22:58:11.369Z
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog-jackett-manifests
  namespace: argocd
  labels:
    infraweaver.io/type: catalog-app
    infraweaver.io/source: community-apps
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: http://onedev.onedev.svc.cluster.local/InfraWeaver-platform
    targetRevision: HEAD
    path: kubernetes/catalog/jackett/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: jackett
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
