"use client";
// Stub for the addon page-loader (P2). The live page is rendered at
// src/app/(dashboard)/wordpress/[site]/page.tsx, which mounts SiteDetailView.
export default function WordpressSiteDetailPage() {
  return null;
}
