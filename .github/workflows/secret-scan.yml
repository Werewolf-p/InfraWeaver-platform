name: Secret Scanning

# Scans for leaked secrets on every push and pull request.
# Uses TruffleHog to detect credentials, API keys, tokens, and other secrets.

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

permissions:
  contents: read
  security-events: write  # required to upload SARIF results

jobs:
  trufflehog:
    name: TruffleHog Secret Scan
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for accurate diff scanning

      - name: TruffleHog Scan
        uses: trufflesecurity/trufflehog@v3.88.28
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --only-verified
