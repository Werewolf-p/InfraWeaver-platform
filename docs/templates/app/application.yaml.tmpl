# ── APP_NAME — ArgoCD ApplicationSet auto-discovery descriptor ───────────────
# This file is detected by the ArgoCD ApplicationSet (kubernetes/*/*/application.yaml).
# The ApplicationSet creates an Application named: apps-APP_NAME
#
# Fill in the details for your Helm chart below.
# For raw-manifest-only apps (no Helm), you can delete this file entirely —
# ArgoCD will still deploy everything in manifests/ via the bootstrap Application.
#
# Check deployment status:  https://argocd.int.rlservers.com (Apps > apps-APP_NAME)
# ─────────────────────────────────────────────────────────────────────────────
repoURL: https://charts.example.com    # TODO: replace with your chart repo URL
targetRevision: "1.0.0"               # TODO: pin to an exact version (never use "*")
chart: APP_NAME                        # TODO: replace with chart name
releaseName: APP_NAME
namespace: apps-APP_NAME
